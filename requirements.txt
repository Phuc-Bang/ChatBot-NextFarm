# Ung dung (app/) - PoC Bai toan A
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
PyYAML==6.0.2

# numpy: 4 module dung TRUC TIEP (embedding/base.py, embedding/local.py,
# retrieval/vector.py, evaluation/runners/eval_retrieval.py) nhung truoc
# 2026-08-28 khong khai o dau ca - no vao may theo duong phu thuoc gian tiep.
numpy==2.2.1

# KENH VECTOR VA RERANKER KHONG NAM O DAY - xem requirements-ml.txt.
#
# sentence-transformers keo theo torch (~2.5GB ban CUDA) va tai model tu
# HuggingFace lan chay dau. De trong file nay thi:
#   - CI cai nhanh, khong tai model, khong can GPU
#   - `pytest` van chay du 331 ca khong dung DB
# Kien truc da chiu duoc dieu do: pipeline.py:22 boc import trong try/except,
# va hybrid.tim_kiem() van chay bang FTS + trigram khi kenh vector hong, chi
# in mot dong canh bao. Do la suy giam CO Y BAO, khong phai im lang.

# Kiem thu
pytest==8.3.4
pytest-asyncio==0.25.0

# httpx: starlette.testclient BAT BUOC co, khong co thi TestClient nem
# RuntimeError chu khong bo qua. Bon test dung no: test_admin_bao_loi,
# test_canh_cua_admin (x2), test_diem_chuyen_gia_phai_that.
#
# THIEU TU TRUOC, bo sung 2026-08-28. Day la ly do CI DO SUOT 13 lan chay: tren
# may nay httpx vao theo duong phu thuoc gian tiep nen khong ai thay, con runner
# sach thi khong co. Cung mot loai loi voi numpy - phu thuoc that nhung khong
# khai, va chi lo ra khi cai tu dau.
httpx==0.28.1
